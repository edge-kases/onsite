apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: phylo-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_USER
          value: "phylo"
        - name: POSTGRES_PASSWORD
          value: "phylo"
        - name: POSTGRES_DB
          value: "phylo"
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: phylo-system
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
